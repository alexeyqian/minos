#include "_fs.h"
#include <minos/fs.h>

void svc_fs(){
    printx(">>> svc_fs is running.\n");
    //for(int i = 0; i < 3; i++)
    //    printx(">>> get ticks: %d\n", kc_ticks());
    while(TRUE){}
}